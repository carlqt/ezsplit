enum UserState {
  GUEST
  AUTHENTICATED
}

# Gets the current user
type Me {
  id: ID!
  username: String!
  totalPayables: String!
  receipts: [Receipt!]!
  state: UserState!
} 

extend type Query {
  me: Me! @authenticated
}
